<Project>
    <Target Name="BuildSass" AfterTargets="AfterResolveReferences">
        <SassTask SassSources="@(Sass)" ToolsPath="$(MSBuildThisFileDirectory)../tools">
            <Output ItemName="EmbeddedResource" TaskParameter="GeneratedFiles" />
        </SassTask>
    </Target>
</Project>